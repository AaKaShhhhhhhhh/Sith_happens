# Supabase — create a project at https://supabase.com, then Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ElevenLabs — only needed to (re)generate suspect voice lines via `pnpm gen:audio`.
# Not read by the browser app; the generated .mp3s live in public/audio.
ELEVENLABS_API_KEY=
# Optional voice-id overrides:
# VOICE_CTO=
# VOICE_INTERN=
# VOICE_SENIOR_DEV=

# Optional analytics (Plausible / PostHog / Cloudflare) — see task 12
